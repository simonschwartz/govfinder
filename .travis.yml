language: node_js
node_js:
  - '7'
install:
  - yarn install
script:
  - yarn test
before_deploy:
  - yarn build
deploy:
  skip_cleanup: true
  provider: surge
  project: ./build/
  domain: govfinder.surge.sh
  on:
    branch: master
